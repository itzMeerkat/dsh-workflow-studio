# Add Workflow Studio to the official global panel composition. Studio
# registers no nodes; node plugins such as dsh-workflow-demo-node supply them.
- insert:
    - id: workflow-studio
      name: dsh-workflow-studio
